# CapCut-like WASM FFmpeg Video Editor Component

Client-side video editing Web Component leveraging `@ffmpeg/ffmpeg` (WASM).

## Features
- In-browser video slicing, trimming, and stitching.
- Lightweight binary loading via WebAssembly.
- Custom controls and timeline component.
