# Senkron Backend Service (`backend/`)

Backend API service handling routing, rate limiting, and REST + GraphQL endpoint execution.

## Key Modules
- **`routers/`**: Route definitions and request controllers.
- **`middleware/`**: Rate limiting and security middleware.
- **`graphql/`**: GraphQL schema definitions and resolvers.
- **`rest/`**: REST API endpoints for Web Components.
